#include<stdio.h>
/**
 * main - Entry point
 * Description - Print numbers of single digits
 * Return: 0 (Success)
 */
int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
	{
		printf("%d", b);
	}
	printf('\n');
	return (0);
}
